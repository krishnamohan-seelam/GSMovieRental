@model PagedList.IPagedList<GSMovieRental.Models.Movie>

@{
    ViewBag.Title = "Movie";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2> Search Movies  Here </h2>

<!-- data-movies-search="@Url.Action("AutoComplete")"-->

<p>
    @using (Ajax.BeginForm(new AjaxOptions
    {
        HttpMethod = "get",
        InsertionMode = InsertionMode.Replace,
        UpdateTargetId = "ReviewList"
    }))

    {


        <div class="row">
            <div class="col-lg-6">
                <div class="input-group">

                    <input class="form-control" id="search" name="search" type="text"
                           value="@ViewBag.CurrentFilter" />
                    <div class="input-group-btn">
                        <button type="submit" class="btn btn-default">
                            <i class="glyphicon glyphicon-search"></i>
                        </button>
                    </div>

                </div>
            </div>
        </div>


    }



</p>

<br />

   @* @Html.ActionLink("Add Movie", "Create")*@



@Html.Partial("_SearchView", Model)
